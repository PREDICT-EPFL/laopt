Improvements in order:

- add ability to define user-specified jacobians and hessians
- add estimation of jacobians and hessians via finite-difference for validation
- add compile-time checks on variable overruns to the iterator and con_t classes

- seperate out generation types (eigen, python, etc)


- test case: car
-- implement array variables (sin, cos)
-- basic manipulations: dot product, transpose


Later
- overload everything so that it can be evaluated in python (with casadi?) for test
- hessians of constraints too... for hessian of lagrangian
- call from python

- descriptors in C++
-- generate names in python to link to the "eq_x" names in c++
-- Add text description to C++
-- get by text name in C++
-- add comment descriptors for each constraint / function in C++


TODO Errors
- Shape of sliced objects is wrong
- bind: Call a function with some args being parameters and some differentiable vars
- Deal with inf in variable bounds properly
- be able to edit the elements of a variableset's lb/ub without changing all the other elements
-- this is happening because they all have the same name
- check if a constraint only involves constants, and then don't generate it

TODO Features / improvements
- functions with multiple outputs (?)
- add generation code and wrappers to call from python\
- add variable sets back in
- drop all variables from the generation that aren't used some constraint
- basic nonlinear functions (sin, cos, division, etc)
- collect dependencies into different print buffers so they can be split out in order
- add a __le__ and __ge__ to generate inequalties
- allow setting of initial dual points
- add hooks to the end of all NLP functions to allow user customization in C
- use the correct variable names in functions
- stack variables from different vectors z = [x[-1];u[2]]
- allow np-like assignments to contmatrices
- add transpose for all variables and non-constant matrices
- make sure everything is overloaded, so that all functions can be called with numpy types and be evaluated
- make VariableSets writable

Format improvements:
- macro out "template segment" and "template cast"
- 

Optimizations
- look for repeated expressions and buffer them into temporary variables
- reuse temporary variables and allocate at the top of the function
- switch to a delegate formulation
- keep a set of generated constants to re-use, rather than generating more (e.g., for lower bounds from numpy, etc)
- generate linear and nonlinear constraints seperately

Checks
- Confirm that functions only use the variables in their argument list
- Confirm that substitutions are valid sizes (perhaps shift to a post-construction validation?)




Options:
- generate lb/ub or Ax<b
- generate lb <= x <= ub seperatly, or include in the general constraints






Extraction of Eigen code:
- generate_array
- 
N, n, m = 10, 2, 1

opt = LOpt()
x = opt.Variable("x", n, N)
u = opt.Variable("u", m, N - 1)
xss = opt.Variable("xss", n)
uss = opt.Variable("uss", m)
r = opt.Parameter("r")


for i in range(N):
    opt.add(x[i+1] == f(x[i]), u[i])
    opt.add(x[i][0] >= 1)
    opt.add(-1 <= u[i] <= 1)
opt.add(xss == f(xss, uss))

opt.minimize(sum())



Expression: Capture to expression trees
NLP: Set of expressions

Eigen: Convert from Expression to 
-- evaluation
-- jacobian
-- declaration
-- initialization


Transpiler
- expression => code


Targets
- ansi c
- ansi c + blas + lapack
- Eigen
- python
- matlab

Notions to be transpiled:
- class
- function
- evaluation
- jacobian (?)
- hessian (?)








top matter

class
	members

	functions

	constructor



Types of things that we need to generate:

- define
	Variables: Produce the int x = 4; statement
	Functions: Produce void f(x) {}; statement
	Expression: Nothing

- eval: Return string evaluating the item.
	Variables: Return name
	Functions: Return matrix tmp; f(x, tmp); tmp
	Expression: Return string evaluation

- initialization: 
	Two types... in 






Tree of generation scopes
- global
	- header material
	- class
		- function
		- function

generation => file
	- scope




Merging preprint + generator into one:

on creation: open file

preprint is then the "internal" buffers
-> open/close class
-> open/close function

on final exit: close file



















=============================================

C++ philosophy:

- functions take inputs (non-contiguous) and return the value, jacobian and /or hessian (in a contiguour block)
- variables are just compile-time offsets into the main variable
- constraints know how to take the output from a function call and copy it into the right location (but don't know how to call the function)


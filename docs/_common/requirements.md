laOPT requires:

- A C++17-compatible compiler
- CMake 3.21 or later
- Eigen 3.4 or later (see [How to install Eigen](installation_eigen))

The core package is header-only. External nonlinear and quadratic programming solvers are required only when their corresponding interface headers are used.

